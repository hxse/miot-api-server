# 阶段计划

## 阶段 1：冻结基础真值与启动安全基线

- 收口项目真值归属边界。
- 冻结技术路线、`uv run` 启动 contract 与最小公网安全 contract。

验收条件：

- `00_meta.md`、`01_context/`、`02_spec/` 与当前执行目标一致。

## 阶段 2：落地第一阶段最小闭环

- 新建 Python 项目入口与 FastAPI 应用。
- 落地启动时 `APP_TOKEN` 校验、健康检查和默认鉴权中间件。
- 落地专用登录页、登录初始化、设备发现与设备级控制主链。

验收条件：

- 应用可被 `uv run` 加载。
- 缺失 `APP_TOKEN` 时启动失败。
- 路由集合与当前 task 的 `02_spec` 保持一致。

## 阶段 3：收口设备能力导向模型

- 收口设备为一级资源、能力为子路由的控制模型。
- 收口设备列表返回结构、页面交互与错误语义。
- 收口同步 provider 路由的执行模型。

验收条件：

- 设备能力导向路由与页面行为符合当前 `02_spec`。

## 阶段 4：统一 task 与回填验收

- 回填执行记录、验收结论与残余风险。

验收条件：

- `04_review/` 已记录结果。
